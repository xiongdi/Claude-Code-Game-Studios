---
paths:
  - "src/ui/**"
---

# UI 代码规则

- UI 绝不能拥有或直接修改游戏状态 — 仅负责显示，使用命令/事件请求变更
- 所有 UI 文本必须通过本地化系统 — 不允许硬编码面向用户的字符串
- 所有交互元素必须同时支持键盘/鼠标和手柄输入
- 所有动画必须可跳过，并尊重用户的动态/无障碍偏好
- UI 声音通过音频事件系统触发，而非直接播放
- UI 绝不能阻塞游戏线程
- 可扩展文本和色盲模式是强制性的，不是可选的
- 必须在最小和最大支持的分辨率下测试所有屏幕
