# 源代码目录

在此目录中编写或编辑游戏代码时，请遵循这些标准。

## 引擎版本警告

LLM 的训练数据早于固定的引擎版本。
**使用任何引擎 API 之前始终检查 `docs/engine-reference/`。**
不要猜测截止日期后的 API 签名 — 先查文档。

## 编码标准

- 所有公共 API 需要文档注释
- 游戏值必须是**数据驱动**（外部配置文件），永远不要硬编码
- 优先使用依赖注入而非单例以提高可测试性
- 每个新系统需要在 `docs/architecture/` 中有对应的 ADR
- 提交必须引用相关的 story ID 或设计文档

## 文件路由

根据正在编写的文件类型匹配合适的引擎专家 Agent。
参见 `CLAUDE.md` → 技术偏好 → 引擎专家 → 文件扩展路由。

有疑问时，使用 `CLAUDE.md` 中配置的主要引擎专家。

## 测试

测试位于 `tests/` — 不在 `src/` 中。
如果测试框架尚不存在，运行 `/test-setup` 搭建。
每个游戏系统都应该有覆盖其公式和边缘情况的单元测试。

## 验证驱动开发

添加游戏系统时先写测试。
对于 UI 更改，用截图验证。
在标记工作完成之前比较预期输出与实际输出。
